
import os
#
from .... import global_var

fpath_tmp = os.path.join(global_var.path_transformed,
                         'eCO2mix',
                         'production_{0}_{1}.csv',
                         )
