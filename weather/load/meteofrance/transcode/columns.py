
import global_var

columns = {'date'      : global_var.weather_dt_UTC,
           't'         : global_var.weather_temperature_kelvin,
           'n'         : global_var.weather_nebulosity,
           'ff'        : global_var.weather_wind_speed,
           'numer_sta' : global_var.weather_site_id,
           'ID'        : global_var.weather_site_id,
           'Nom'       : global_var.weather_site_name,
           'Latitude'  : global_var.geography_latitude,
           'Longitude' : global_var.geography_longitude,
           }
